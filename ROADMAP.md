# מערכת ניהול בוטיק — מסמך סטטוס חי

> מסמך זה מתעדכן תוך כדי העבודה. אם פותחים שיחה חדשה — לקרוא אותו קודם כדי לדעת איפה הכל עומד.

## מידע כללי
- **פרויקט Lovable** מחובר ל-GitHub: `https://github.com/elyaumalka/comprehensive-blueprint`
- **מקומי:** `C:\Users\elika\comprehensive-blueprint`
- **Supabase project id:** `purwojkfhsdvxeerddlv` — SQL editor: https://supabase.com/dashboard/project/purwojkfhsdvxeerddlv/sql/new
- **טכנולוגיה:** React 19 + TanStack Router/Start + Vite + Tailwind + shadcn/ui + Supabase. RTL מלא בעברית.
- **עיצוב:** לבן נקי, אדום (alert) להתראות בלבד, זהב (gold) לאקצנט. כרטיסי פירוט נפתחים כ**פופאפ ממורכז** (Dialog) עם גלילה — לא Sheet מהצד.

## דרך העבודה (חשוב!)
1. Claude עורך קוד מקומית ב-git, עושה `commit` + `push` ל-`main`.
2. Lovable מסנכרן אוטומטית מ-GitHub (לוקח דקה-שתיים) — צריך לרענן.
3. **לא לערוך קוד ב-Lovable במקביל** — יוצר התנגשויות git (כבר קרה פעם אחת, נפתר ב-rebase).
4. **שינויי מסד נתונים (migrations) לא מוחלים אוטומטית** — צריך להריץ את ה-SQL ידנית ב-Supabase SQL editor. התראת "destructive" על המילה DELETE בתוך GRANT/פונקציה — בטוח ללחוץ Run anyway.
5. בכל קומיט: לא כוללים `package-lock.json` (npm יוצר רעש; הפרויקט על bun). כן כוללים `routeTree.gen.ts` כשמוסיפים route חדש.

## SQL שכבר הורץ ✅
- מודול 1 (כרטיס עובד): `referred_by_employee_id`, `employee_expenses`, `overtime_minutes`
- מודול 3 (יעדים): טבלת `business_goals`
- מודול 11 (היסטוריה): טריגרי `audit_log`

## SQL שצריך להריץ ⚠️
- **מודול 13 (הרשאות)** — 2 שלבים לפי הסדר:
  1. `ALTER TYPE public.app_role ADD VALUE IF NOT EXISTS 'accounting';` + `'marketing';`
  2. רק אחרי שהשלב הראשון רץ — קובץ `20260602150100_roles_policies.sql` (פוליסי גישה להנהח"ש ושיווק)

---

## ✅ מודולים שהושלמו ונדחפו (15)
1. **כרטיס עובד מלא** — שעות (יומי/חודשי/שנתי), שעות נוספות, מכירות, סה"כ סכומים, עמלת משווקת (לפי לקוחות שהופנו), הוצאות נוספות (שי/מתנה/בונוס), דוח שנתי משווה, הדפסה/PDF. + שדה "הופנתה ע"י עובדת" בכרטיס לקוח.
2. **כרטיס לקוח מורחב** — מגזר(7)/מקור פרסום(9)/סוג אירוע(6)/שפה, שם ממליצה מותנה, זיהוי לקוחה חוזרת אוטומטי לפי טלפון → VIP אוטומטי, היסטוריית רכישות, חוק נעילה (אי-מחיקה, רק לא-פעיל).
3. **יעדים עסקיים + דשבורד בזמן אמת** — KPIs מתרעננים, פסי התקדמות מול יעד (חודשי/וואטסאפ/לפי עובדת), דיאלוג הגדרת יעדים.
4. **החזרות/החלפות/ביטולים** — מסך מלא, מעקב סטטוס, החזר/זיכוי, חזר למלאי, חוק "אין החזר בלי סיבה".
5. **דשבורד מלאי** (בעמוד מוצרים) — מוצרים, יחידות, מלאי נמוך, אזל, שווי מלאי.
6. **דוחות וניתוחים** (/reports) — מכירות (חודשי+בוקר/ערב+תשלומים), השוואת עובדות, מקורות הגעה, החזרות, הוצאות. גרפים + בורר שנה + הדפסה.
7. **יומן ומשימות** — סטטוס(פתוח/בטיפול/ממתין/הושלם), שיוך לעובדת, סינון (הכל/היום/באיחור/הושלמו), **תצוגת לוח שנה חודשי + הוספה בלחיצה על יום**.
8. **זרימת הזמנות** — טאבים פעילות/שבוצעו, תיעוד תאריך בכל שינוי סטטוס (status_history).
9. **חיפוש חכם גלובלי** (Ctrl+K) — לקוחות/מוצרים/מספר הזמנה, בראש כל מסך.
10. **מכירה חדשה מלאה** — פריטים מהקטלוג, הנחות (אחוז/סכום + סיבה חובה), מע"מ, תשלומים, ביטול-עם-סיבה (חוק נעילה).
11. **היסטוריית שינויים** (/history) — טריגרים שמתעדים כל שינוי (מי/מה/מתי) + עמוד צפייה עם סינון.
12. **שעון נוכחות** — התחלת/סיום משמרת בראש המסך, חסימת מכירה ללא משמרת פתוחה. + תיקון RTL גלובלי בטבלאות (`text-start`).
13. **הרשאות מורחבות** — 5 תפקידים (מנהלת/מוכרת/הנהח"ש/שיווק/צופה), קישור login↔עובדת (לשעון), תפריט צד לפי תפקיד. (דורש SQL — ראה למעלה)
14. **כרטיס מוצר מלא** — כל השדות + ניהול וריאציות מידה×צבע×מלאי, סטטוס, שיווק ארץ/חו"ל.
15. **תנועות מלאי** (/inventory) — רישום תנועה (כניסה/מכירה/החלפה/החזרה/תיקון/גריעה/העברה) + עדכון מלאי אוטומטי.
16. **הוספת משתמשת (login)** ממסך ההגדרות — יצירת חשבון דרך client זמני + הקצאת תפקיד.
17. **דוחות נוספים** — רווח נקי חודשי (הכנסות מול הוצאות), 15 מוצרים מובילים, לקוחות/CLV/חוזרות/VIP, ביטולים והנחות חודשיים.
18. **מיזוג כפילויות לקוח** — איחוד 2 כרטיסים (העברת מכירות/הזמנות/החזרות + סימון כפול כלא פעיל).
19. **הוצאות מתקדם** — שיוך לספק, צירוף מסמך, אמצעי תשלום/תשלומים, טאב "קבלות חסרות" (להגשה דו-חודשית).
20. **ציר זמן בכרטיס לקוח** — הערות מערכת אוטומטיות (רכישות, ביטולים, שינויים, חוזרת/וואטסאפ).

---

## 📌 החלטות פתוחות (הלקוחה תחליט)
- **מכירות מול הזמנות:** כרגע נפרדים לחלוטין. הכנסה מתשלומי **הזמנות** עדיין לא נספרת בדוחות/דשבורד (רק טבלת `sales`). אופציות: (א) לכלול תשלומי הזמנות בדוחות, (ב) ליצור מכירה אוטומטית כשהזמנה נמסרת, (ג) להשאיר. — **ממתין להחלטה**.

## 🔨 נשאר לבנות (בלי חיבורים חיצוניים)
- 🔗 **ניהול קולקציה** (קישורי OneDrive)
- 🏆 **מועדון לקוחות** (צריך הגדרה מהלקוחה מה הרעיון)
- 🌍 פילוח הכנסות ארץ/חו"ל (דורש סימון מדינה לכל מכירה)

## 🔌 דורש חיבור חיצוני (נשאר לסוף — אין עדיין שירותים)
- 💳 סליקת אשראי
- 📧 מייל אוטומטי (קבלות, טופס הזמנה, "מוכן לאיסוף") + ניוזלטר
- 💬 אוטומציית וואטסאפ (צירוף לקבוצה, הודעות דו-לשוניות עברית/אנגלית)
- 🧾 קבלות אוטומטיות (מורנינג)
- 📅 סנכרון Google Calendar
- 🔔 התראות push בתאריכים
